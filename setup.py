from setuptools import setup, find_packages

setup(
    name="timeutils",
    version="0.0.1",
    packages=find_packages(),
    description="Various utils for time-based work.",
    classifiers=["Programming Language :: Python :: 3.12"],
)
